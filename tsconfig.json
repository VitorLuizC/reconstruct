{
  "compilerOptions": {
    "target": "es2015",

    // Settings for module.
    "module": "es2015",
    "allowSyntheticDefaultImports": true,

    // Settings for generating type definitions.
    "strict": true,
    "declaration": true,
    "declarationDir": "types/"
  },
  "files": ["src/index.ts"],
  "exclude": ["dist/", "types/"]
}
